import XCTest
@testable import ESVBible

final class ESVBibleTests: XCTestCase {
    func testAppLaunches() throws {
        // Basic test to verify the app module can be imported
        XCTAssertTrue(true)
    }
}
