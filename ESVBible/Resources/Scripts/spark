#!/bin/bash
if [ $# -eq 0 ]; then
    open -a Spark
else
    ref="$*"
    # Convert "Hebrews 4:12" → spark://Hebrews/4/12
    # Replace spaces with / and : with /
    url=$(echo "$ref" | sed 's/ /\//g; s/:/\//g')
    open "spark://$url"
fi
