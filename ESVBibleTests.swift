import XCTest
@testable import Spark

final class SparkTests: XCTestCase {
    func testAppLaunches() throws {
        // Basic test to verify the app module can be imported
        XCTAssertTrue(true)
    }
}
